# OpenAI Configuration (optional - for AI chatbot features)
# Managed by Replit integration - use the integration setup
OPENAI_API_KEY=

# Node Environment
NODE_ENV=development

# Server Configuration (if using backend)
PORT=3000
API_PORT=3001

# Replit Environment (auto-configured)
# REPLIT_DOMAINS=your-repl-domain.replit.dev

# Email Configuration (optional)
ADMIN_EMAIL=jeffemuodafe124@gmail.com
